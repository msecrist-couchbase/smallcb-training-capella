title: Query Rows
page: page-03
lang: nodejs
infoBefore: |
  <div>
  <a href='basic-nodejs-kv-get' class="prev-button">Previous Lesson</a>
  <a href='basic-nodejs-query-named-param' class="next-button">Next Lesson</a>
  </div>
  <br>
code: |
  const couchbase = require('couchbase')

  const cluster = new couchbase.Cluster('couchbase://{{.Host}}', {
    username: '{{.CBUser}}', password: '{{.CBPswd}}'
  })

  cluster.bucket('travel-sample')

  const getHotelsByCity = async (city) => {
    try {
      const query = `
        SELECT name, city, state
        FROM \`travel-sample\`
        WHERE type = 'hotel'
          AND city = $CITY LIMIT 5
      `
      const options = {
        parameters: {
          CITY: city
        }
      }
      let results = await cluster.query(query, options)
      results.rows.forEach((row) => {
        console.log('Result row: ', row)
      })
      return results
    } catch (err) {
      console.error(err)
    }
  }

  getHotelsByCity('Malibu')
    .then(() => process.exit(0))
infoAfter: |
  Basic N1QL query,
  with looping through each returned row.
  <br/><br/>
  Visit the docs to learn more about
  <a target="_blank" href="https://docs.couchbase.com/nodejs-sdk/current/howtos/n1ql-queries-with-sdk.html#handling-results">Handling Query Results in Node</a>.
