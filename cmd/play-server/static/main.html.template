<!doctype html>
<html>
<head>
  <title>Couchbase API Playground</title>
</head>
<body>
  <style>
    .flex-row {
      display: flex;
      flex-direction: row;
    }

    .main {
      margin-bottom: 80px;
      border-top: 1px solid #666;
    }

    .main .main-left {
      flex-grow: 1;
      margin-right: 20px;
    }

    .main .main-center {
      flex-grow: 10;
    }

    input, textarea, pre {
      display: block;
      margin-bottom: 10px;
    }

    textarea {
      width: 95%;
      height: 20em;
      overflow-y: scroll;
      background-color: #333;
      color: #eee;
      font-family: courier, monospace;
    }

    .output {
      width: 90%;
      font-family: courier, monospace;
    }

    .footer {
      border-top: 1px solid #666;
      padding-top: 10px;
      font-size: 10pt;
      color: #666;
    }
  </style>

  <h1>Couchbase API / SDK Playground</h1>

  <div class="main flex-row">
    <div class="main-left">
      <h2>Examples</h2>

      <ul class="langs">
      {{range .NameTitles}}
        <li>
          <a href="/?name={{.Name}}">{{.Title}}</a>
        </li>
      {{end}}
      </ul>
    </div>

    <div class="main-center">
      <h2>Playground</h2>

      <div class="item">
        <form class="item-form"
              action="run" method="post" target="output">
          <input name="lang"
                 type="hidden" value="{{.Lang}}"/>

          <input type="submit" value="run"/>

          <textarea name="code"
                    autocorrect="off"
                    autocomplete="off"
                    autocapitalize="off"
                    spellcheck="false">{{.Code}}</textarea>

          <input type="submit" value="run"/>
        </form>
      </div>
    </div>

    <div class="main-right">
      <h2>Output</h2>

      <iframe name="output"></iframe>
    </div>
  </div>

  <div class="footer">
    powered by the smallcb project
  </div>
</body>
</html>
